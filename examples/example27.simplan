{
    /*
    This example shows the errors returned when one tries to return pointers through functions.
    The program should run into a type checking error and should display:
      - "Functions cannot return pointers." 
    */
    ^bool returnPointer() {
        return new bool;
    }

    ^bool p = returnPointer();
}